#!/bin/sh /etc/rc.common

START=99
STOP=10
USE_PROCD=1

start_service() {
	procd_open_instance
	procd_set_param command /usr/libexec/fancontrol-loop
	procd_set_param respawn
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}

stop_service() {
	# 退出时交还内核控制，避免停机后风扇卡在手动占空比
	rm -f /tmp/fancontrol_reload
}
